# Top Friends

A mobile-first web app. Currently a hello world.

Built with React + Vite.

## Development

```sh
npm install
npm run dev
```

## Other scripts

```sh
npm run build    # production build to dist/
npm run preview  # serve the production build locally
npm run lint     # oxlint
```
