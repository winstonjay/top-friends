# Supabase project settings -> API. Copy to .env.local (git-ignored).
# Only ever the anon (publishable) key here — the service_role key must
# never reach client code.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
